const skills = [
    { name:"HTML/CSS", category:"frontend" },
    { name:"JavaScrip", category:"frontend" },
];

export const SkillsSection = () => {
    return (
        <section>

        </section>
    )
}