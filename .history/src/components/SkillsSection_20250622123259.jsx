const skills = [
    { name:"HTML/CSS", category:"frontend" },
    { name:"JavaScripth", category:"frontend" },
];

export const SkillsSection = () => {
    return (
        <section>

        </section>
    )
}