import { Description } from "@radix-ui/react-toast"

const projects = [
    {
        id: 1,
        title: "Maltiks's portfolio",
        Description: 
    }
]

export const ProjectsSection = () => {
    return (
        <section>

        </section>
    )
}