const skills = [
    { name:"HTML/CSS", category:"frontend" },
    { name:"HTML/CSS", category:"frontend" },
];

export const SkillsSection = () => {
    return (
        <section>

        </section>
    )
}